# Jev x OpenClash Smart Controller
基于 TypeSafe AI Jev 模型的 OpenClash 智能决策面板。

## FnOS / 服务器部署方法
1. 确保安装了 Docker 和 Docker Compose。
2. 在该目录下运行 `docker-compose up -d --build`。
3. 访问 `http://<服务器IP>:8000` 即可查看面板。
